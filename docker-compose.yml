services:
  teiko:
    build: .
    ports:
      - "3000:3000"
    environment:
      PORT: "3000"
      DATA_DIR: "/app/data"
      UPLOAD_DIR: "/app/data/uploads"
      VISUAL_SETTINGS_WRITE: "${VISUAL_SETTINGS_WRITE:-1}"
      ADMIN_PATH: "${ADMIN_PATH:-/test-admin-teiko}"
      ADMIN_PASSWORD: "${ADMIN_PASSWORD:-admin123}"
    volumes:
      - teiko-data:/app/data
    restart: unless-stopped

volumes:
  teiko-data:
